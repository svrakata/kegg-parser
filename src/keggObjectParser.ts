// read line by line
// extract label
// if no label add the current line with the indentation to the last label's content

export const keggObjectParser = (rawKeggObject: string) => {
    const lines = rawKeggObject.split("\n")
}

